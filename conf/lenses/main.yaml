main:
  Regions: ${union:${union:${lenses.regional.regions},[IAUM]}}
  Sectors: ${union:[ARKW,IBIT,AIQ,SHLD], ${get_keys:tickers.sectors}}
  Stable: ${union:${lenses.sectoral.Commodity},[AVDV, AVUV, IDMO, SPMO, AVES, EUFN, IXG, EXI, IXN, IXP]}

zoo:
  GlobalX: ${get_keys:tickers.globalX}
  ARK: ${get_keys:tickers.ark}
  InvescoMomentum: ${get_keys:tickers.momentum}

regional:
  regions: [CEE, ILF, AFK, AIA, ASEA, IPAC, FEP, SPMO, IDMO, PIE, EMM]
  countries: ${get_keys:tickers.regions}

sectoral:
  Commodity: ${get_keys:tickers.commodities}
  Industrials: [EXI, SHLD, NATO, ITA, EUAD]
  Financial: [IXG, EUFN, KIE, KMPR, PGR, ALL, BRK-B, TRV]
  Utility: [IXC, JXI, AES, VST, NEE, DUK, FLNC]
  Technology: [IXN, AIQ, IXP, PTF, MSFT, AAPL, GOOGL, NVDA, AMZN]
