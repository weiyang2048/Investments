main:
  Regions: ${lenses.regional.regions}
  Sectors: ${union:[ARKW, BKCH, DMAT, HYDR, URA, LIT, AIQ,SHLD], ${get_keys:tickers.sectors}}
  Stable: ${union:${lenses.regional.USxUS},[CORO], ${get_keys:tickers.sectors}}

zoo:
  globalX: ${get_keys:tickers.globalX}
  ARK: ${get_keys:tickers.ark}
  invescoMomentum: ${get_keys:tickers.momentum}

regional:
  USxUS: [SPMO, IDMO, FEM, AVUV, AVDV, AVES, IAUM, IBIT]
  regions: ${union:${lenses.regional.USxUS},[CEE,  FLN, AFK, AIA, ASEA, IPAC, FEP]}
  countries: ${union:${lenses.regional.USxUS},${get_keys:tickers.regions}}
# sectoral:
#   Industrials: [EXI, SHLD, NATO, ITA, EUAD]
#   Financial: [IXG, EUFN, KIE, KMPR, PGR, ALL, BRK-B, TRV]
#   Utility: [IXC, JXI, AES, VST, NEE, DUK, FLNC]
#   Technology: [IXN, AIQ, IXP, PTF, MSFT, AAPL, GOOGL, NVDA, AMZN]
