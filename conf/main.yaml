lenses:
  main:
    Regions: ${union:[SPMO, IDMO,EEMO, CEE,  FLN, AFK, AIA, ASEA, IPAC, FEP], [IAUM,  SLV, PPLT, IBIT, ETHA], [VNAM, EWY, EPU, EWK, NFTY, EPOL, EZA, EIS, ARGT]}
    Sectors: ${union:[PYZ, PTH, PUI], [ARKW, ARKQ, ARKG], [QTUM, REMX], [BKCH, GOEX, SLVP, COPX, DMAT, HYDR, URA, LIT, AIQ, SHLD], ${get_keys:tickers.sectors}}
    Commodities: [IAUM, GOEX, SLV, SLVP, PPLT, COPX, REMX, DMAT, HYDR, URA, LIT, IEO, DBA, BTC, ETH, BKCH ]
    Stocks: ${union:[AVGO, CAT], [ASM, ARGX], [OPEN, LCID]}
    r: [IAUM, VEU, GOOGL, SPMO, AIA]
    f: ${union:[SPMO, BKCH, IAUM, GOEX, SLVP, ARKQ, TRAMX, AIQ, XMMO, SHLD, AIA, JXI, IXN], ${lenses.main.Stocks}}
  themes:
    regions: ${union:[SPMO, IDMO, FEM, AVUV, AVDV, AVES],${get_keys:tickers.regions}}
    income: [YGLD, MAXI, BPI, BCCC,  SCHD, SCHY,LVHI, LVHD, GPIQ, GPIX, JEPI, JEPQ, DIVO, IDVO, EFAS, SDEM]
    sp50: ${get_keys:tickers.stocks.sp50}
    adr50: ${get_keys:tickers.stocks.adr50}
    ARK: ${get_keys:tickers.ark}
    invescoMomentum: ${get_keys:tickers.momentum}
    globalX: ${get_keys:tickers.globalX}

defaults:
  - style_conf: [shared_configs, color_line_styles]
  - tickers:
      - etfs
      - sectors
      - regions
      - commodities
      - globalX
      - momentum
      - ark
      - stocks/sp50
      - stocks/adr50